Open LibreOffice Writer on this machine. Type the sentence "Hello from agent-desktop test" into a new, empty document. Save the document as `/tmp/eval-writer.odt`. Then exit.

Use the most efficient path you can. The bench will check for the file's existence and that the saved content contains the expected sentence.
